#!/usr/bin/env bash
# nerf-az-aks-nodepool-list -- List node pools on an AKS cluster.
# Generated from az-aks manifest. Do not edit directly.
# nerf:threat:read=remote
# nerf:threat:write=none

if [[ "${BASH_VERSINFO[0]:-0}" -lt 4 ]]; then
  echo "error: nerf-az-aks-nodepool-list requires bash 4+. Found bash ${BASH_VERSION:-unknown}" >&2
  echo "  hint: on macOS, install a newer bash via 'brew install bash'" >&2
  exit 1
fi

set -euo pipefail

NERFTOOLS_BRAND="nerf"

_NERF_DRY_RUN=""

usage() {
  cat >&2 <<'EOF'
Usage: nerf-az-aks-nodepool-list --resource-group|-g <resource_group> [--subscription <subscription>] <cluster_name>

Options:
  --resource-group, -g <resource_group> (required)
      Resource group containing the cluster
  --subscription <subscription>
      Subscription name or ID (defaults to active)

Arguments:
  <cluster_name> (required)
      AKS cluster name

Maps to: az aks nodepool list --resource-group <resource_group> --cluster-name <cluster_name> <subscription> --output json

List node pools on an AKS cluster.
EOF
  exit 1
}

RESOURCE_GROUP=""
_RESOURCE_GROUP_SET=""
SUBSCRIPTION=""
_SUBSCRIPTION_SET=""

while [[ $# -gt 0 ]]; do
  case "$1" in
    --resource-group|-g) if [[ -n "${_RESOURCE_GROUP_SET}" ]]; then echo "error: --resource-group can only be specified once" >&2; exit 1; fi; RESOURCE_GROUP="$2"; _RESOURCE_GROUP_SET=true; shift 2 ;;
    --subscription) if [[ -n "${_SUBSCRIPTION_SET}" ]]; then echo "error: --subscription can only be specified once" >&2; exit 1; fi; SUBSCRIPTION="$2"; _SUBSCRIPTION_SET=true; shift 2 ;;
    --nerf-dry-run) _NERF_DRY_RUN="true"; shift 1 ;;
    -h|--help) usage ;;
    --) shift; break ;;
    *) break ;;
  esac
done

_CLUSTER_NAME_SET=""
if [[ $# -gt 0 ]]; then
  CLUSTER_NAME="$1"
  _CLUSTER_NAME_SET=true
  shift
else
  CLUSTER_NAME=""
fi
if [[ $# -gt 0 ]]; then
  echo "error: nerf-az-aks-nodepool-list: unexpected extra arguments: $*" >&2
  echo "  hint: switches and options must come before positional arguments" >&2
  exit 1
fi

for _bin in az; do
  if ! command -v "$_bin" >/dev/null 2>&1; then
    echo "error: nerf-az-aks-nodepool-list: required command '$_bin' is not installed or not on PATH" >&2
    exit 127
  fi
done

if [[ -z "${RESOURCE_GROUP}" ]]; then
  echo "error: nerf-az-aks-nodepool-list: missing required option --resource-group" >&2
  echo "  hint: provide --resource-group <value>" >&2
  usage
fi

if [[ -n "${_CLUSTER_NAME_SET}" ]] && [[ "${CLUSTER_NAME}" == -* ]]; then
  echo "error: nerf-az-aks-nodepool-list: <cluster_name> cannot start with '-'" >&2
  echo "  hint: use -- before positional arguments if needed" >&2
  exit 1
fi

if [[ -z "${CLUSTER_NAME}" ]]; then
  echo "error: nerf-az-aks-nodepool-list: missing required argument <cluster_name>" >&2
  echo "  hint: provide a value for <cluster_name>" >&2
  usage
fi

if [[ "$_NERF_DRY_RUN" == "true" ]]; then
  _NERF_DRY_CMD=(az aks nodepool list --resource-group "${RESOURCE_GROUP}" --cluster-name "${CLUSTER_NAME}" ${_SUBSCRIPTION_SET:+"--subscription"} ${_SUBSCRIPTION_SET:+"$SUBSCRIPTION"} --output json)
  printf 'dry-run:'
  for _a in "${_NERF_DRY_CMD[@]}"; do printf " %q" "$_a"; done
  echo
  exit 0
fi

exec az aks nodepool list --resource-group "${RESOURCE_GROUP}" --cluster-name "${CLUSTER_NAME}" ${_SUBSCRIPTION_SET:+"--subscription"} ${_SUBSCRIPTION_SET:+"$SUBSCRIPTION"} --output json
