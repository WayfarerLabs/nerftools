version: 1

package:
  name: az-keyvault
  description: Azure Key Vault inspection tools (network ACLs, secret metadata, masked secret access)
  skill_group: az-keyvault
  skill_intro: |
    Tools for inspecting Key Vault configuration and secret metadata.
    az-keyvault-show reports network ACLs, public network access, RBAC mode,
    and private endpoint connections. az-keyvault-secret-list returns secret
    names only (no values). For confirming a secret value matches expectations
    without exposing it, use az-keyvault-secret-stats (length, charset class,
    sha256 prefix) or az-keyvault-secret-show-masked (first/last 3 chars).
    All tools accept --subscription to target a specific subscription.

tools:
  az-keyvault-list:
    description: List Key Vaults (optionally filtered by resource group)
    threat:
      read: remote
      write: none
    template:
      command:
        - az
        - keyvault
        - list
        - "{{options.resource_group}}"
        - "{{options.subscription}}"
        - --output
        - json
    options:
      resource_group:
        description: Filter to a specific resource group
        flag: --resource-group
        short: -g
      subscription:
        description: Subscription name or ID (defaults to active)
        flag: --subscription

  az-keyvault-show:
    description: Show Key Vault details (network ACLs, publicNetworkAccess, RBAC mode, private endpoints)
    threat:
      read: remote
      write: none
    template:
      command:
        - az
        - keyvault
        - show
        - --name
        - "{{arguments.name}}"
        - "{{options.resource_group}}"
        - "{{options.subscription}}"
        - --output
        - json
    arguments:
      name:
        description: Key Vault name
        required: true
    options:
      resource_group:
        description: Resource group containing the vault (optional, helps with disambiguation)
        flag: --resource-group
        short: -g
      subscription:
        description: Subscription name or ID (defaults to active)
        flag: --subscription

  az-keyvault-network-rule-list:
    description: List explicit network rules on a Key Vault
    threat:
      read: remote
      write: none
    template:
      command:
        - az
        - keyvault
        - network-rule
        - list
        - --name
        - "{{arguments.vault_name}}"
        - "{{options.resource_group}}"
        - "{{options.subscription}}"
        - --output
        - json
    arguments:
      vault_name:
        description: Key Vault name
        required: true
    options:
      resource_group:
        description: Resource group containing the vault (optional)
        flag: --resource-group
        short: -g
      subscription:
        description: Subscription name or ID (defaults to active)
        flag: --subscription

  az-keyvault-secret-list:
    description: List secret names in a Key Vault (no values)
    threat:
      read: remote
      write: none
    template:
      command:
        - az
        - keyvault
        - secret
        - list
        - --vault-name
        - "{{arguments.vault_name}}"
        - "{{options.subscription}}"
        - --output
        - json
    arguments:
      vault_name:
        description: Key Vault name
        required: true
    options:
      subscription:
        description: Subscription name or ID (defaults to active)
        flag: --subscription

  az-keyvault-secret-stats:
    description: >-
      Show fingerprint stats for a secret value (length, charset class,
      sha256 prefix, content type, last modified) without revealing any
      characters. Use to confirm a secret was rotated or matches a known
      shape, without exposing the value.
    threat:
      read: remote
      write: none
    guards:
      - command: [which, jq]
        fail_message: 'jq is required but not installed (e.g. apt-get install jq, brew install jq).'
      - command: [which, sha256sum]
        fail_message: 'sha256sum is required but not installed (typically part of GNU coreutils).'
    script: |
      ARGS=(az keyvault secret show --vault-name "${VAULT_NAME}" --name "${NAME}" --output json)
      if [[ -n "${SUBSCRIPTION}" ]]; then
        ARGS+=(--subscription "${SUBSCRIPTION}")
      fi
      RESPONSE=$("${ARGS[@]}") || {
        echo "error: az-keyvault-secret-stats: failed to fetch secret '${NAME}' from vault '${VAULT_NAME}'" >&2
        exit 1
      }
      VALUE=$(printf '%s' "${RESPONSE}" | jq -r '.value')
      CONTENT_TYPE=$(printf '%s' "${RESPONSE}" | jq -r '.contentType // ""')
      UPDATED=$(printf '%s' "${RESPONSE}" | jq -r '.attributes.updated // ""')
      LEN=${#VALUE}
      if [[ "${VALUE}" =~ ^[A-Fa-f0-9]+$ ]]; then
        CHARSET="hex"
      elif [[ "${VALUE}" =~ ^[A-Za-z0-9+/]+=*$ ]]; then
        CHARSET="base64"
      elif [[ "${VALUE}" =~ ^[A-Za-z0-9_-]+$ ]]; then
        CHARSET="urlsafe"
      elif [[ "${VALUE}" =~ ^[A-Za-z0-9]+$ ]]; then
        CHARSET="alphanumeric"
      elif [[ "${VALUE}" =~ [[:cntrl:]] ]]; then
        CHARSET="contains-control"
      elif [[ "${VALUE}" =~ [^[:print:]] ]]; then
        CHARSET="contains-nonprint"
      else
        CHARSET="mixed-printable"
      fi
      FINGERPRINT=$(printf '%s' "${VALUE}" | sha256sum | cut -c1-12)
      jq -n \
        --arg vault "${VAULT_NAME}" \
        --arg name "${NAME}" \
        --argjson length "${LEN}" \
        --arg charset "${CHARSET}" \
        --arg fingerprint "${FINGERPRINT}" \
        --arg content_type "${CONTENT_TYPE}" \
        --arg updated "${UPDATED}" \
        '{vault: $vault, name: $name, length: $length, charset_class: $charset, sha256_prefix: $fingerprint, content_type: $content_type, last_modified: $updated}'
    arguments:
      vault_name:
        description: Key Vault name
        required: true
      name:
        description: Secret name
        required: true
    options:
      subscription:
        description: Subscription name or ID (defaults to active)
        flag: --subscription

  az-keyvault-secret-show-masked:
    description: >-
      Show first 3 and last 3 characters of a secret value, plus length and
      last modified. For values shorter than 12 characters, the masked output
      omits the prefix/suffix to avoid revealing too much.
    threat:
      read: remote
      write: none
    guards:
      - command: [which, jq]
        fail_message: 'jq is required but not installed (e.g. apt-get install jq, brew install jq).'
    script: |
      ARGS=(az keyvault secret show --vault-name "${VAULT_NAME}" --name "${NAME}" --output json)
      if [[ -n "${SUBSCRIPTION}" ]]; then
        ARGS+=(--subscription "${SUBSCRIPTION}")
      fi
      RESPONSE=$("${ARGS[@]}") || {
        echo "error: az-keyvault-secret-show-masked: failed to fetch secret '${NAME}' from vault '${VAULT_NAME}'" >&2
        exit 1
      }
      VALUE=$(printf '%s' "${RESPONSE}" | jq -r '.value')
      UPDATED=$(printf '%s' "${RESPONSE}" | jq -r '.attributes.updated // ""')
      LEN=${#VALUE}
      if (( LEN < 12 )); then
        MASKED="(value too short to mask safely)"
      else
        PREFIX="${VALUE:0:3}"
        SUFFIX="${VALUE: -3}"
        MASKED="${PREFIX}...${SUFFIX}"
      fi
      jq -n \
        --arg vault "${VAULT_NAME}" \
        --arg name "${NAME}" \
        --argjson length "${LEN}" \
        --arg masked "${MASKED}" \
        --arg updated "${UPDATED}" \
        '{vault: $vault, name: $name, length: $length, masked: $masked, last_modified: $updated}'
    arguments:
      vault_name:
        description: Key Vault name
        required: true
      name:
        description: Secret name
        required: true
    options:
      subscription:
        description: Subscription name or ID (defaults to active)
        flag: --subscription
